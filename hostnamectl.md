# Hostnamectl (Hostname setzen) 

```
# Abfragen 
hostnamectl
hostnamectl set-hostname centos4.training.local  
hostnamectl 
# Trick für prompt - ist in der aktuellen, erst nach neueinloggen/bzw. neuer bash aktiv 
su - root # bzw. su - benutzer 
```
